{
  "name": "letlotlo-la-temo",
  "version": "0.1.0",
  "private": true,
  "description": "Letlotlo la Temo mobile and backend MVP.",
  "workspaces": [
    "backend",
    "mobile",
    "apps/web"
  ],
  "scripts": {
    "backend:dev": "npm --workspace backend run dev",
    "backend:build": "npm --workspace backend run build",
    "backend:seed": "npm --workspace backend run seed",
    "mobile:start": "npm --workspace mobile run start",
    "mobile:android": "npm --workspace mobile run android",
    "mobile:ios": "npm --workspace mobile run ios"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "brace-expansion": "^1.1.11",
    "cosmiconfig": "^5.2.1",
    "hasown": "^2.0.2",
    "resolve-from": "^4.0.0"
  }
}
